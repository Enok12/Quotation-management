export const metadata = {
  title: "MONTRA — Customer Registration",
  description: "Submit your details to work with MONTRA Clothing & Manufacturing",
};

export default function CustomerFormLayout({ children }: { children: React.ReactNode }) {
  return children;
}
