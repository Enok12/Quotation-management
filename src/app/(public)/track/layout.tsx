export const metadata = {
  title: "MONTRA — Order Tracking",
  description: "Track your MONTRA order's production status",
};

export default function TrackLayout({ children }: { children: React.ReactNode }) {
  return children;
}
